import React from 'react';

const Footer = () => {
  return (
    <footer className="foot">
      <small> &copy; {new Date().getFullYear()} CitySpire</small>
    </footer>
  );
};

export default Footer;
